# nobiscuit - Game Design Document

## Game Overview

nobiscuit は TUI（ターミナル）で動くレイキャスティング 3D 迷路ゲーム。
DOOM / Wolfenstein 風の一人称視点を、ハーフブロック文字と 24bit True Color で完全にターミナル内に描画する。

## Concept

**nobiscuit** = no biscuit = のび助（nobisuke）+ ビスケット

### Story

ドラえもんの「ホーム迷路」のような、無限に続く家の中を彷徨う。
各部屋に散らばるビスケットを全て食べて（消して）この世から無くしていく。
全てのビスケットを消し、迷路を脱出した時、自分がのび助だったことに気づく。タイトル回収がダジャレ。

### Why TUI?

- テクスチャの知識がない → テクスチャ不要の方向に振り切る
- TUI のレイキャスティング 3D は他にほぼ存在しない → それ自体が差別化
- Claude Code はテクスチャ付き 3D のデザインが苦手だった（前回の教訓）
- WebGL/Phaser はホットリロードが効かず開発体験が悪かった

## Game System

### Movement

- Wizardry 風のグリッド移動。1 マスずつ、90 度ずつ
- 位置はタイル中央に固定。壁に引っかかることがない
- 前進・後退・左右回転。後退は向きを変えずに後ろに下がる
- アニメーション付きスライド（瞬間移動ではない）

### Controls

| Key | Action |
|---|---|
| W / Up | 前進（1マス） |
| S / Down | 後退（1マス） |
| A / Left | 左を向く（90度） |
| D / Right | 右を向く（90度） |
| M | ミニマップ ON/OFF |
| Q / Esc | 終了 |

### Hunger System

- 空腹ゲージ（1.0 = 満腹、0.0 = 餓死）
- 時間経過で減少（約 50 秒で餓死）
- ビスケットを拾うと 0.15 回復（上限 1.0）
- ゲージは HUD 左上にバーで表示。緑→黄→赤で色が変わる
- スコア・経験値・レベルなし。食うか死ぬか出るか

### Sprites

- **ビスケット**: 丸いクッキー型の AA パターン。壁の 1/4 の高さ。床に接地
- **ゴール**: 同サイズの光る球体。少し浮いている
- 距離に応じて自動スケーリング（近い = 大きい AA、遠い = ドット）
- パターンの透明部分からは背景が見える

### Minimap

- 半透明オーバーレイ（alpha 0.4）。3D 画面が透けて見える
- 画面右端にフラッシュ、上下は中央揃え
- プレイヤーは赤い点、向いている方向は黄色い線
- M キーで ON/OFF 切り替え

### Win/Lose Conditions

- 全ビスケットを消してゴールに到達 → 脱出（エンディング）
- 空腹ゲージが 0 → 餓死（ゲームオーバー）

## Windows

- 原作「ホーム迷路」の再現要素。家の中なので窓がある
- 窓は壁と同じく通り抜け不可だが、見た目が異なる（木枠 + ガラス面 + 十字格子）
- ミニマップ上では青色で表示
- 将来的に「移動可能な壁/窓」として、迷路が動的に変化する仕組みの基盤

## Multiple Floors

- 3 階建ての家。階段で上下移動
- 各階は独立した迷路で、それぞれにビスケットが配置される
- 階段上り（↑矢印スプライト）と階段下り（↓矢印スプライト）
- HUD 左上にフロア表示（例: "2F" + ドットインジケータ）
- 階移動時にメッセージ表示（"Going up to 2F..."）
- ゴールは最上階のみ（全階を探索する必然性を生む）

## Minimap Visibility (Design Notes)

現在はデバッグ用途で常時表示可能（M キーでトグル）。
本番リリースでは視認制限を設けて「迷子の恐怖感」を演出する。

検討中のルール案:
- **アイテム制**: 特定のアイテムを拾うと一定時間だけマップが見える
- **回数制限**: マップを見られる回数に上限がある（例: 3回）
- **フロアごとにリセット**: 新しい階に行くとマップ表示回数が補充される
- **ビスケットと引き換え**: マップを見るたびに空腹度が減る
- **部分表示**: 全体マップではなく、自分の周囲だけ表示される

## Maze Generation

- **不定形マスクベース**: 正方形固定ではなく、各階で異なる有機的な形状
- マスク生成: 2-4 個のシード点から BFS でアメーバ状に拡張（全ノードの 40-70%）
- マスク外のセルは `TILE_VOID`（壁でも通路でもない虚無。真っ暗に表示）
- **島（連結成分）**: 同じ階に複数の独立した島が存在しうる
- **部屋配置**: 2x2〜4x3 の小部屋をマスク内にランダム配置（最大80回試行、重複禁止、壁1枚隣接は許可）
- 各島で独立に DFS 迷路生成。部屋内のノードは pre-carved として扱い、DFS が自然に部屋と廊下を接続
- **可変幅通路**: DFS 後に内壁の 15-25% を候補として壁除去を試行。flood fill で連続空間が 12 マスを超える場合は復元
- 同一階の島同士は直接行き来不可（迷子感の演出）
- 階段で別の階の別の島に出ることで、島を渡り歩く構造
- 現在のサイズ: 31 x 25 タイル
- ゴールは最大の島の最も遠い空きマスに自動配置
- ビスケットは空きマスの約 30% にランダム配置
- 内壁の約 15% がランダムに窓に置換される

## Visual Design

### Rendering Pipeline

1. フレームバッファをクリア
2. レイキャスティング（列ごとに 1 本の ray）
3. 床・天井の描画（パースペクティブ正確な座標計算）
4. 壁の描画（テクスチャ付き）
5. スプライトの描画（AA パターン、深度テスト付き）
6. ミニマップのオーバーレイ（半透明ブレンド）
7. HUD（空腹バー、メッセージ）
8. デルタフラッシュでターミナルに出力

### Wall Textures (Procedural)

- 板壁の縦溝（タイル端で暗くなる）
- 長押（なげし）— 壁の上部 35% に水平レール
- 幅木（はばき）— 壁の下部に暗い帯
- 木目模様 — 壁面水平位置に基づく縦の波紋
- 横板の継ぎ目 — 壁を 3 分割する薄い線
- タイル座標ハッシュで壁ごとに微妙な色違い

### Floor & Ceiling (Mode7-Style)

- パースペクティブ正確なワールド座標計算
- 床: 畳/板張りのグリッド線 + 板目ライン + タイルごとの色ムラ
- 天井: 控えめなパネルグリッド
- 距離に応じてパターン密度が自然に変化（近い = 粗い、遠い = 密）

### Color Palette

| Element | Color |
|---|---|
| Wall (vertical hit) | rgb(180, 140, 100) + hue variation |
| Wall (horizontal hit) | rgb(160, 125, 85) + hue variation |
| Floor | rgb(74, 60, 40) |
| Ceiling | rgb(135, 206, 235) |
| Biscuit | rgb(220, 180, 80) |
| Goal | rgb(50, 220, 50) |
| Player (minimap) | rgb(255, 50, 50) |
| Direction (minimap) | rgb(255, 255, 0) |
